# 控制器概述

- 控制器负责处理路由命中的请求与生成响应，命名空间建议为 `App\\<Module>\\Controller`。
- 方法参数支持路由具名参数与请求对象注入，返回 `string` 或 `HtmlResponse`。
- 统一使用 `PhotonixCore\\Request` 获取 `GET/POST/PUT/DELETE/PATCH/OPTIONS` 参数与头、Cookie、文件等。