# Photonix View 概述

- 支持 Smarty 风格模板语法，保留自定义插值标记 `{{ ... }}` 与代码标记 `{% ... %}`。
- 模板默认目录：`views/`；后缀通过配置 `view_suffix`（默认 `.tpl`）。
- 编译产物缓存到 `runtime/cached`，按模板绝对路径 `md5` 命名，自动增量更新。
- 控制器向模板传参：`View::display('path', 'cached', ['k' => v])` 或全局赋值 `View::assign('a.b', v)`。