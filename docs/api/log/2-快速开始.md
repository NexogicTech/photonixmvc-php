# 快速开始

```
use PhotonixCore\Logger;
Logger::info('用户登录', ['uid' => 123]);
Logger::error('数据库异常', ['code' => 500]);
Logger::debug('请求参数', ['q' => $_GET]);
```

- 默认写入 `runtime/log/app-YYYY-mm-dd.log`
- 在 `config/log.php` 中启用并设置最小级别