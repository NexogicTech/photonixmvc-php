<?php

namespace PhotonixCore\View;

class View
{
    public $config = require_once ROO
    /**
     * @param string $tpl_path 渲染的模板文件
     */
    public static function display(string $tpl_path, $type = "cached"): string
    {
        if ($type == "cached") {

        } elseif ($type == "return") {
            return ;
        }
        return  ;
    }
}